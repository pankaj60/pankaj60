#include <stdio.h>
int main()
{
    int a[5],i;
    printf("Enter the  array  elements");
    for(i=1;i<=5;i++)
    {
    scanf("%d",&a[i]);
    }
    printf("Reverse  array  elements");
    for(i=5;i>=1;i--)
    {
        printf("\n %d",a[i]);
    }
    
    return 0;
}
