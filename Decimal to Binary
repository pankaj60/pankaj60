

#include <stdio.h>
int main()
{
    int[10],n,i;
    printf("Enter the number you want to convert:-");
    scanf("%d",&n);
    for(i=0;n>0;i++)
    {
        a[i]=n%2;
        n=n\2
    }
    printf("the binary number is:-\n");
    for(i=i-1;i>=0i--);
    {
        printf ("%d",a[i]);
    }
    
    return 0;
}
